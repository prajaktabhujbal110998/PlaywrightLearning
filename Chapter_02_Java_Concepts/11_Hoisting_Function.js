//Step 1: Defination of the function
function getUserStatus(){

//var status_code = undefined; //not shown to you 
console.log(status_code);
var status_code = "Active";
console.log(status_code);

}

//Step 2: Calling of the function
getUserStatus();   
    

//Note: var is function-scoped, so status_code is hoisted to
//The top of the getUserStatus(), not the global scope
