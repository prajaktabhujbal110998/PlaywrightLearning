//let class = "Hello World";
// //class is a reserved keyword in JavaScript, so it cannot be used as an identifier. This will result in a syntax error.

l//et if = true;
// //if is a reserved keyword in JavaScript, so it cannot be used as an identifier. This will result in a syntax error.

//let return = 5;
// //return is a reserved keyword in JavaScript, so it cannot be used as an identifier. This will result in a syntax error.

//let myVariable = 10;
// This is a valid identifier and can be used without any issues.

// //They look like keyword but aren't reserved keywords
// let undefined = 5;  //Works, but NEVER do this!
//let Infinity = 10; //Works, but NEVER do this!